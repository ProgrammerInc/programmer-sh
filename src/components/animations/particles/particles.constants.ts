/**
 * Constants for the Particles animation component
 */

/**
 * Default number of particles to render
 */
export const DEFAULT_PARTICLE_COUNT = 200;

/**
 * Default spread factor for particle distribution
 */
export const DEFAULT_PARTICLE_SPREAD = 10;

/**
 * Default animation speed factor
 */
export const DEFAULT_SPEED = 0.1;

/**
 * Default particle colors (hex format)
 */
export const DEFAULT_PARTICLE_COLORS = ['#ffffff', '#ffffff', '#ffffff'];

/**
 * Default setting for whether particles react to mouse position
 */
export const DEFAULT_MOVE_PARTICLES_ON_HOVER = false;

/**
 * Default strength of hover effect
 */
export const DEFAULT_PARTICLE_HOVER_FACTOR = 1;

/**
 * Default setting for whether particles have alpha transparency
 */
export const DEFAULT_ALPHA_PARTICLES = false;

/**
 * Default base size of particles
 */
export const DEFAULT_PARTICLE_BASE_SIZE = 100;

/**
 * Default randomness factor for particle sizes
 */
export const DEFAULT_SIZE_RANDOMNESS = 1;

/**
 * Default distance of camera from particles
 */
export const DEFAULT_CAMERA_DISTANCE = 20;

/**
 * Default setting for whether to disable automatic rotation
 */
export const DEFAULT_DISABLE_ROTATION = false;

/**
 * CSS class name for the particles container
 */
export const PARTICLES_CONTAINER_CLASS = 'particles-container';

/**
 * Default initial mouse position
 */
export const DEFAULT_MOUSE_POSITION = { x: 0, y: 0 };

/**
 * Default camera field of view
 */
export const DEFAULT_CAMERA_FOV = 15;

/**
 * Default rotation speed factors
 */
export const ROTATION_SPEED = {
  x: 0.0002,
  y: 0.0005,
  z: 0.01
};

/**
 * Default rotation amplitude factors
 */
export const ROTATION_AMPLITUDE = {
  x: 0.1,
  y: 0.15
};
