/**
 * Constants for the Particle Network animation component
 */

/**
 * Default number of particles
 */
export const DEFAULT_PARTICLE_COUNT = 50;

/**
 * Default size of particles in pixels
 */
export const DEFAULT_PARTICLE_SIZE = 2;

/**
 * Default color for particles
 */
export const DEFAULT_PARTICLE_COLOR = '#4a90e2';

/**
 * Default color for connection lines
 */
export const DEFAULT_LINE_COLOR = 'rgba(74, 144, 226, 0.2)';

/**
 * Default maximum distance for connections
 */
export const DEFAULT_MAX_DISTANCE = 100;

/**
 * Default movement speed
 */
export const DEFAULT_SPEED = 1;

/**
 * Default interactive state
 */
export const DEFAULT_INTERACTIVE = true;

/**
 * CSS class name for the particle network container
 */
export const PARTICLE_NETWORK_CONTAINER_CLASS = 'particle-network-container';

/**
 * Frame rate control (milliseconds per frame)
 */
export const ANIMATION_FRAME_RATE = 1000 / 60; // 60 FPS

/**
 * Minimum edge distance for bouncing particles
 */
export const EDGE_BOUNCE_MARGIN = 0;
