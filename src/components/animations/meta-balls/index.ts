/**
 * Meta Balls Animation Component
 *
 * An interactive metaball animation using WebGL shaders that displays
 * fluid-like blobs that merge together when they get close to each other.
 *
 * @module MetaBalls
 */

// Don't export the component directly to ensure proper lazy loading
// Use the LazyMetalBalls component from @/lib/lazy-components instead
export type { MetaBallsProps } from './meta-balls.types';
