/**
 * Lightning animation component module.
 *
 * Provides a WebGL-based animation effect with dynamic, lightning-like patterns
 * that can be customized with different colors, speeds, and intensities.
 *
 * @module Lightning
 */

export { Lightning, default } from './lightning';
export type { LightningProps } from './lightning.types';
