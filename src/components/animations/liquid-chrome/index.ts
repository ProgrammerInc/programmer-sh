/**
 * Liquid Chrome Animation Component
 *
 * A WebGL-based animation that creates a liquid chrome effect with interactive
 * distortions that respond to mouse/touch input.
 *
 * @module LiquidChrome
 */

export { LiquidChrome, default } from './liquid-chrome';
export type { LiquidChromeProps } from './liquid-chrome.types';
