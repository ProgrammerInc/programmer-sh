/**
 * Default text to display in the HyperspaceHero component
 */
export const DEFAULT_TEXT = '30k';

/**
 * Default gradient for the text color
 */
export const DEFAULT_TEXT_COLOR = 'linear-gradient(135deg, #8a2be2, #ff69b4)';

/**
 * Default number of stars to render in the animation
 */
export const DEFAULT_STAR_COUNT = 400;

/**
 * Default animation speed factor
 */
export const DEFAULT_SPEED = 2;

/**
 * Default CSS class for the container
 */
export const DEFAULT_CLASS_NAME = '';

/**
 * Maximum animation frame reference
 */
export const INITIAL_ANIMATION_REF = 0;

/**
 * Star color generation parameters
 */
export const STAR_COLOR_PARAMS = {
  redBase: 180,
  redVariation: 75,
  greenBase: 180,
  greenVariation: 75,
  blueBase: 220,
  blueVariation: 35,
  opacityMin: 0.6,
  opacityVariation: 0.4
};

/**
 * Star size parameters
 */
export const STAR_SIZE_PARAMS = {
  minSize: 0.5,
  sizeVariation: 1.5,
  maxDisplaySize: 5
};

/**
 * Position factor for star rendering
 */
export const POSITION_FACTOR = 200;

/**
 * Maximum star tail length calculation factor
 */
export const MAX_TAIL_LENGTH = 30;
