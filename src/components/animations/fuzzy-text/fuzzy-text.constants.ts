/**
 * Constants for the FuzzyText component
 */

/** Default font size */
export const DEFAULT_FONT_SIZE = 'clamp(2rem, 8vw, 8rem)';

/** Default font weight */
export const DEFAULT_FONT_WEIGHT = 900;

/** Default font family */
export const DEFAULT_FONT_FAMILY = 'inherit';

/** Default text color */
export const DEFAULT_COLOR = '#fff';

/** Default hover effect enablement */
export const DEFAULT_ENABLE_HOVER = true;

/** Default base intensity for the fuzzy effect */
export const DEFAULT_BASE_INTENSITY = 0.18;

/** Default hover intensity for the fuzzy effect */
export const DEFAULT_HOVER_INTENSITY = 0.5;

/** Default range for the fuzzy effect in pixels */
export const DEFAULT_FUZZ_RANGE = 30;

/** Default horizontal margin in pixels */
export const DEFAULT_HORIZONTAL_MARGIN = 50;

/** Default vertical margin in pixels */
export const DEFAULT_VERTICAL_MARGIN = 0;

/** Default extra width buffer in pixels */
export const DEFAULT_WIDTH_BUFFER = 10;
