/**
 * Constants for the Mesh Matrix animation component
 */

/** Default color for the mesh wireframe */
export const DEFAULT_MESH_COLOR = '#ffffff';

/** Default density of the mesh grid */
export const DEFAULT_MESH_DENSITY = 30;

/** Default intensity of the distortion effect */
export const DEFAULT_DISTORTION_INTENSITY = 0.5;

/** Default CSS class name */
export const DEFAULT_CLASS_NAME = '';

/** Default camera position */
export const DEFAULT_CAMERA_POSITION: [number, number, number] = [0, 0, 5];

/** Default camera field of view */
export const DEFAULT_CAMERA_FOV = 75;

/** Default mesh scale */
export const DEFAULT_MESH_SCALE: [number, number, number] = [1, 1, 1];

/** Default material opacity */
export const DEFAULT_MATERIAL_OPACITY = 0.3;

/** Default wave frequency factor for distortion */
export const DEFAULT_WAVE_FREQUENCY = 0.3;

/** Default wave amplitude for base distortion */
export const DEFAULT_WAVE_AMPLITUDE = 0.1;

/** Default maximum distance for mouse influence */
export const DEFAULT_MAX_MOUSE_DISTANCE = 2;
