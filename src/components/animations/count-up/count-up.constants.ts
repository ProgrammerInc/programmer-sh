/**
 * Constants for the CountUp component
 */

/**
 * Default starting value
 */
export const DEFAULT_FROM = 0;

/**
 * Default animation direction
 */
export const DEFAULT_DIRECTION = 'up';

/**
 * Default delay before animation starts (in seconds)
 */
export const DEFAULT_DELAY = 0;

/**
 * Default animation duration (in seconds)
 */
export const DEFAULT_DURATION = 2;

/**
 * Default CSS class name
 */
export const DEFAULT_CLASS_NAME = '';

/**
 * Default animation start state
 */
export const DEFAULT_START_WHEN = true;

/**
 * Default number separator
 */
export const DEFAULT_SEPARATOR = '';

/**
 * Default number format locale
 */
export const DEFAULT_LOCALE = 'en-US';

/**
 * Default animation damping multiplier
 * Used to calculate damping based on duration
 */
export const DAMPING_MULTIPLIER = 40;

/**
 * Default animation damping base value
 */
export const DAMPING_BASE = 20;

/**
 * Default animation stiffness multiplier
 */
export const STIFFNESS_MULTIPLIER = 100;
