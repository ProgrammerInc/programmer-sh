/**
 * Constants used by the Aurora Canvas component
 *
 * @module AuroraCanvas
 */

/**
 * Default colors for aurora particles
 */
export const DEFAULT_COLORS = ['#00ff87', '#60efff', '#0061ff', '#ff0099'];

/**
 * Default movement speed for particles
 */
export const DEFAULT_SPEED = 0.2;

/**
 * Default number of aurora layers
 */
export const DEFAULT_LAYERS = 3;

/**
 * Default particle density per layer
 */
export const DEFAULT_PARTICLE_DENSITY = 25;

/**
 * Default maximum distance for mouse interaction in pixels
 */
export const DEFAULT_INTERACTIVE_DISTANCE = 200;

/**
 * Default strength of mouse interaction effect
 */
export const DEFAULT_INTERACTIVE_FORCE = 0.01;

/**
 * CSS class names used in Aurora Canvas component
 */
export const CSS_CLASSES = {
  canvas: 'block h-full w-full'
};
