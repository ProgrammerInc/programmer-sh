/**
 * Constants for the Magnet Lines animation component
 */

/** Default number of rows in the grid */
export const DEFAULT_ROWS = 9;

/** Default number of columns in the grid */
export const DEFAULT_COLUMNS = 9;

/** Default size of the container (CSS value) */
export const DEFAULT_CONTAINER_SIZE = '80vmin';

/** Default color of the lines (CSS color value) */
export const DEFAULT_LINE_COLOR = '#efefef';

/** Default width of each line (CSS value) */
export const DEFAULT_LINE_WIDTH = '1vmin';

/** Default height of each line (CSS value) */
export const DEFAULT_LINE_HEIGHT = '6vmin';

/** Default base angle for the lines in degrees */
export const DEFAULT_BASE_ANGLE = -10;

/** Default CSS class name */
export const DEFAULT_CLASS_NAME = '';

/** Default inline styles */
export const DEFAULT_STYLE = {};
