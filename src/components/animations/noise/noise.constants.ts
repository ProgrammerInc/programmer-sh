/**
 * Constants for the Noise animation component
 */

/**
 * Default size of the noise pattern in pixels
 */
export const DEFAULT_PATTERN_SIZE = 250;

/**
 * Default horizontal scale factor for the pattern
 */
export const DEFAULT_PATTERN_SCALE_X = 1;

/**
 * Default vertical scale factor for the pattern
 */
export const DEFAULT_PATTERN_SCALE_Y = 1;

/**
 * Default number of frames before refreshing the noise pattern
 */
export const DEFAULT_PATTERN_REFRESH_INTERVAL = 2;

/**
 * Default alpha transparency value for the noise pattern (0-255)
 */
export const DEFAULT_PATTERN_ALPHA = 15;

/**
 * CSS class name for the noise canvas element
 */
export const NOISE_CANVAS_CLASS = 'noise-canvas';
