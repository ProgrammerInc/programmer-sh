/**
 * Blob Cursor Component
 *
 * Exports an interactive cursor component that displays animated blob elements
 * that follow the mouse cursor with a trailing effect.
 *
 * @module BlobCursor
 */

export { BlobCursor, default } from './blob-cursor';
export * as BlobCursorStyles from './blob-cursor.module.css';
export type { BlobCursorProps } from './blob-cursor.types';
