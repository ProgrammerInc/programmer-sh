/**
 * This file re-exports buttonVariants for backward compatibility
 * This allows other components to continue using the old import path
 */

import { buttonVariants } from './button.variants';

export { buttonVariants };
